fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0

# LangGraph dependencies with version-specific imports
# Note: langgraph-checkpoint-postgres==1.0.9 uses PostgresSaver (not AsyncPostgresSaver or PostgresCheckpointer)
# Correct import: from langgraph.checkpoint.postgres import PostgresSaver
langgraph==0.2.16
langgraph-checkpoint==1.0.12
langgraph-checkpoint-postgres==1.0.9

# PostgreSQL drivers
psycopg[binary,pool]>=3.1.0

# Verification command:
# python -c "from langgraph.graph import StateGraph; from langgraph.checkpoint.postgres import PostgresSaver; print('✓ LangGraph imports successful')"
